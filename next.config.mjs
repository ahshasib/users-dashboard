/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export', // এটি বলে যে site static export হবে
    distDir: 'out',   // export হওয়া folder
  }
  
  module.exports = nextConfig
  